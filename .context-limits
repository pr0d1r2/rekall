# Per-path token ceilings, gated by `itok check` on every commit.
#
# Counts are TOKENIZER-backed (o200k, itok's pinned `--bpe` tier), never a
# bytes/4 heuristic and never a model's guess. That is what makes a ceiling
# comparable across machines and across days -- an estimator that varies per
# run cannot gate anything. rekall does not implement the counter: itok owns
# token accounting and this file is read by itok's own runner (SPEC.md V8).
#
# WHAT THIS CEILING IS: a cap on ENTRY COST. SPEC.md is what an agent loads
# before it does any work in this repo, and that cost is re-billed on every
# turn, so it is subtracted from headroom before anything useful happens. It
# is not a fit constraint -- 6,773 tokens fits any window in use.
#
# It forces a REVIEW, not a shrink. The slack below is deliberate (~13%): a
# ceiling set a hair above the current figure turns every legitimate addition
# into a raise, and a threshold raised reflexively trains the reflex instead
# of catching the problem. When a raise is genuinely right, it belongs in a
# commit that says why -- which is the whole reason the number lives in a
# tracked file rather than in a flag.
#
# MEASURED 2026-08-21, `itok estimate --bpe`:
#   SPEC.md    6773
#   README.md   842
#
# RE-MEASURED 2026-08-24, after specifying nine tasks, two bugs and V39:
#   SPEC.md   10272
#
# THE CEILING ROSE AGAIN, 9700 -> 11000, and I argued against exactly this
# one session ago. What changed is that I tried the alternative and
# MEASURED it: compressing the four longest invariants -- V29, V34, V37 and
# V38, all written in the same recent house style -- bought 72 tokens.
# Compression is spent. The driver is the COUNT (39 invariants, 50 tasks),
# not the length of any one of them.
#
# Federating section R was considered and REJECTED. At 162 lines this file
# is far under FORMAT.md's 500-line threshold, and splitting it would be
# the "grep ceremony" that rule exists to prevent -- solving verbosity with
# structure, which is the wrong tool applied loudly.
#
# Slack is ~7%, deliberately tighter than the ~12.5% used before. The next
# addition should NOT be absorbed: it should force merging overlapping
# invariants, which is the debt this number now tracks.
#
# RE-MEASURED 2026-08-22, after the amendment that unblocked T12/T13/T26,
# T19 and T33:
#   SPEC.md    8619
#   README.md   842
#
# CLAUDE.md JOINED THE REGISTRY at 600 against a measured 430. It is an
# always-loaded entry cost like the other two -- an agent reads it before
# doing anything here -- and V12 wants the ceiling to land BEFORE the file
# grows into it, not after the growth ratifies whatever happened. It is
# also the corpus rekall points at itself (T19, T35), so it is the one
# file in this repo whose growth the tool is supposed to reverse.
#
# THE CEILING ROSE, 7700 -> 9700, and this is the review it was built to
# force rather than a number moved to clear a red. What breached it:
# V29-V33, R14, B1-B2, T34-T36 and one §I bullet -- five invariants, a
# research row, the first two bug rows, three tasks. A tightening pass came
# first and bought ~150 tokens; the remaining ~1850 is content, so cutting
# further would have meant deleting reasoning rather than compressing it.
#
# The §B rows are the ones worth defending, because they are the format's
# whole point and the section had been empty: a bug with no row is a bug
# the next session repeats. Every other addition unblocks a task that was
# stalled on a decision nobody had made.
#
# Slack is ~12.5%, the same discipline the first ceiling used. When this
# one is next approached, compact §B before widening again -- oldest rows
# first, per the ONE FILE RULE.
#
# WHY THIS FILE EXISTS AT ALL, stated here because it is the trap it was
# born from: V12 required the ceiling from commit one, and for several
# commits this repo declared one in section C while nothing read it. A
# declaration no runner reads is a wish, and wishes drift silently because
# nothing reports them (V22). The runner landed with the number, in one
# commit, which is what V22 actually asks for.

SPEC.md   11000
README.md  1000
CLAUDE.md   600
